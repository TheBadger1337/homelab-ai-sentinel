# Secrets — *.env covers .secrets.env, .env, .env.production, etc.
*.env

__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/
dist/
.DS_Store
*.log
.pytest_cache/
pyrightconfig.json

# Local notes — not for public repo
MEMORY.md
ROADMAP.md

# Personal infrastructure docs
docs/

# Gumroad guide source content
gumroad-docs/

# Built frontend (generated by Vite build)
static/

# Frontend dependencies
ui/node_modules/

# Tests (contain real network fixtures — kept locally, not in public repo)
tests/

# Real topology and runbooks (contain network details / IPs)
runbooks/topology.yaml
runbooks/*.md

# Claude Code context (per-developer)
CLAUDE.md
.claude/
